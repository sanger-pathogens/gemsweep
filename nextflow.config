// Copyright (C) 2024 Genome Research Ltd.

// Config inheritance options
params {
    generic_config_base = "https://raw.githubusercontent.com/sanger-pathogens/nextflow-commons/"
    generic_config_version = "master"
    generic_config_url = ""
}
inherit_generic_config()

manifest {
    name            = '{{pipeline_name}}'
    author          = 'PAM Informatics'
    homePage        = '{{pipeline_url}}'
    description     = '{{pipeline_description}}'
    mainScript      = 'main.nf'
    nextflowVersion = '>=21.04.0'
    version         = '{{pipeline_version}}'
    recurseSubmodules = true
}

includeConfig "./assorted-sub-workflows/mixed_input/mixed_input.config"

params {
    // general options
    outdir = "./results"
    references = null

<<<<<<< HEAD
<<<<<<< HEAD
<<<<<<< HEAD
    // reference refinement options
    refine_refs = false
    representatives = 20
    cluster_method = 'network_based_trim' // hidden option
=======
=======
    // skani
    skani_db = "/data/pam/software/skani/skani_gtdb_r226-v0.3"
    accession_pattern = ".*/(.*)_genomic.fna.gz"
    ref_db_files = "/data/pam/team230/wr7/scratch/tmp/PAT-3147_sylph_gemsweep/gtdb_genomes_reps_r226_paths.txt"

>>>>>>> 8d2f62c (Extract top hits using skani)
=======
>>>>>>> d1ddc99 (Add sylph profiling and summary outputs for primary/secondary thresholds)
    // sylph (see also taxo_profile asw)
    sketch_size = 31 // Only ksize 21 or 31 supported
    sylph_db = "/data/pam/software/sylph/gtdb-r226-c200-dbv1/gtdb-r226-c200-dbv1.syldb"
    assemblies = null
    sylph_db_custom = null
    sylph_primary_ani = 99
    sylph_primary_cov = 1
    sylph_secondary_ani = 95
    sylph_secondary_cov = 0.7
    // TODO: decide whether to add abundance thresholds (Taxonomic_abundance or Sequence_abundance)
    // i think -u should always be on
    sylph_estimate_unknown = false
    save_sylph_sketches = true
>>>>>>> 2a672a7 (Use sylph to profile)

    // themisto options
    temp_dir = null
    temp_space = 10000
    themisto_index = null
    kmer_size = 31
    
    // msweep options
    ref_groups = null

    // mgems options
    get_assignments = false
    min_abundance = 0.0001

    // poppunk options
    poppunk_model = "dbscan"
    publish_poppunk = false
}

process {
    maxRetries = params.max_retries
    withLabel: request_temp {
        clusterOptions = "-R 'rusage[tmp=${params.temp_space}]'"
    }
}

// Helper functions
def inherit_generic_config() {
    config_url = params.generic_config_url ? params.generic_config_url : "${params.generic_config_base}/${params.generic_config_version}/configs/nextflow.config"
    try {
        includeConfig "${config_url}"
    } catch (Exception e) {
        System.err.println("ERROR: Could not load generic config: ${config_url}")
        System.err.println("Encountered the following exception:")
        throw e
    }
}

