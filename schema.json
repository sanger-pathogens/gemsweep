{
  "pipeline": "Base schema",
  "params": {
    "Logging options": {
      "monochrome_logs": {
        "default": "false",
        "help_text": "Should logs appear in plain ASCII"
      }
    },
    "General options": {
      "manifest": {
        "default": "null",
        "help_text": "Input manifest CSV with (required) header line 'ID,R1,R2', containing per sample paths to .fastq.gz files"
      },
      "outdir": {
        "default": "./results",
        "help_text": "Path to top directory containing all results, by default 'results' within the launch directory."
      },
      "skip_main": {
        "default": "false",
        "help_text": "Run only reference preparatory steps, skipping read pseudoalignment through to binning."
      }
    },
    "References options": {
      "ref_mode": {
        "default": "null",
        "help_text": "Mode for processing of reference or index inputs. Choose from: 'index', 'full', 'refine', 'autoselect'."
      },
      "references": {
        "default": "null",
        "help_text": "Path to text file containing paths to references, one per line. Required when --ref_mode is full or refine. Not used when --ref_mode is autoselect, or when --themisto_index and --ref_groups are provided for index mode."
      },
      "representatives": {
        "default": "20",
        "help_text": "Number of representatives at which to cap each reference cluster."
      },
      "save_sylph_sketches": {
        "type": "boolean",
        "default": false,
        "description": "Publish Sylph sketch files (.sylsp) to the sample-specific sylph output directory."
      }
    },
    "Cache options": {
      "cache_dir": {
        "default": "null",
        "help_text": "Path to a cache root or an existing config-specific cache directory for autoselect mode. The pipeline reuses a matching cache directory containing metadata.json and per-species references.txt/groups.txt entries."
      }
    },
    "Clustering options": {
      "cluster_tool": {
        "default": "poppunk",
        "help_text": "Tool to use for clustering references when ref_mode is `refine` or `full` (poppunk|sketchlib)"
      },
      "poppunk_model": {
        "default": "dbscan",
        "help_text": "Clustering model for poppunk to use (dbscan|bgmm)"
      },
      "publish_poppunk": {
        "default": "false",
        "help_text": "Optionally publish full poppunk output, group assignments are always published."
      },
      "ani_threshold": {
        "default": "0.02",
        "help_text": "Max ANI distance threshold for clustering (default 0.2 clusters genomes sharing >98% ANI similarity)"
      },
      "sketchlib_kstep": {
        "default": "13,29,4",
        "help_text": "Kmer sizes at which sketchlib will sketch the reference in the format start,stop,step"
      },
      "cluster_strict": {
        "default": "false",
        "help_text": "Fail early if all genomes form a single cluster, or each genome is a singleton."
      }
    },
    "Themisto options": {
      "temp_dir": {
        "default": "null",
        "help_text": "Absolute path to custom temporary storage directory to be used during runtime. Otherwise local /tmp will be used."
      },
      "temp_space": {
        "default": "10000",
        "help_text": "Amount of /tmp space (MB) that will be reserved for index creation and pseudoalignment, if /tmp is being used as the temporary storage directory."
      },
      "themisto_index": {
        "default": "null",
        "help_text": "Path to a pre-built Themisto index including the index prefix (without exts). Skips indexing if provided."
      },
      "kmer_size": {
        "default": "31",
        "help_text": "K-mer size to use for index and pseudoalignment (21|31|51). If providing an index ensure k-mer size is identical."
      }
    },
    "mSWEEP options": {
      "ref_groups": {
        "default": "null",
        "help_text": "Grouped references text file, one line per reference. Mandatory when pre-built index is supplied to --themisto_index."
      }
    },
    "mGEMS options": {
      "get_assignments": {
        "default": "false",
        "help_text": "Output the read assignment table used by mGEMS for binning."
      },
      "min_abundance": {
        "default": "0.0001",
        "help_text": "Only bin reads for groups that have a relative abundance higher than this value."
      }
    }
  },
  "overwrite_param": {
    "none": {}
  }
}
